from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='an ml project which analyzes the comments of youtube videos',
    author='areeba rao',
    license='',
)
